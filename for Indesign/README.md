# Scripts InDesign (Longoka Games)

- **Livres grille** : `Longoka_*_Book_FromPack.jsx` (mots mêlés, mots croisés, mots fléchés, morpho-anagrammes). Ils chargent `Longoka_Editions_Config.jsx` (chemins, validation JSON, `LG.setCellStrokeUniform`, `LG.lexiconPolicyTextFromPack`).
- **Couvertures** : voir `README_COVERS.md`.
- **Génération des JSON** : `backend-java/GENERATION.md` (Maven / `BiweeklyPuzzleBatchTool`).

Les raccourcis dans `../scripts jsx for indesign/` ouvrent les scripts ci-dessus via `$.evalFile`.
